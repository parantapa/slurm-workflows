[build-system]
requires = ["setuptools", "setuptools_scm"]
build-backend = "setuptools.build_meta"

[project]
name = "slurm-workflows"
dynamic = ["version"]
authors = [
  { name="Parantapa Bhattacharya", email="parantapa@virginia.edu" },
]
description = "HPC workflow helpers for Slurm clusters."
readme = "README.md"
requires-python = ">=3.12"
classifiers = [
    "Programming Language :: Python :: 3",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]

dependencies = [
    "click",
    "tqdm",
    "jinja2",
    "json5",
    "platformdirs",
    "cloudpickle",
    "typeguard>=3",
    "psutil",
    "numpy",
    # The terminal UI `swtop` runs in.
    "textual",
    # 1.15 is where `qmc.Sobol` took `rng=`, the seed argument used here.
    "scipy>=1.15",
    # 5.0.0 is where `task_search_id` arrived,
    # which is how `swtop` lists the tasks on a server.
    "ds-service-client>=5.0.0"
]

[project.optional-dependencies]
botorch = ["botorch"]
test = ["pytest", "botorch"]
dev = ["black", "pyright"]

[tool.setuptools.packages.find]
where = ["src"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-ra"
# Deprecations are the ones worth failing on:
# they are how a dependency announces a break one release ahead,
# and a warning nobody reads is a break discovered at the worst moment.
# Everything else stays a warning
# --- botorch is deliberately handed a constant objective in places,
# and says so at runtime.
filterwarnings = [
    "error::DeprecationWarning",
    "error::PendingDeprecationWarning",
]

# Pinned to the `requires-python` floor so formatting does not drift
# with whatever Python happens to be running black.
[tool.black]
target-version = ["py312"]

[tool.pyright]
include = ["src", "tests", "examples"]
pythonVersion = "3.12"

[tool.setuptools_scm]
fallback_version = "1.0.0-dev"

[project.urls]
"Homepage" = "https://github.com/parantapa/slurm-hpc-workflows"

[project.scripts]
"slurm-pilot-worker" = "slurm_workflows.slurm_pilot_worker:slurm_pilot_worker"
"swtop" = "slurm_workflows.swtop:swtop"
